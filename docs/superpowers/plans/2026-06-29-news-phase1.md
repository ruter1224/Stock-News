# 新聞功能 (Phase 1) 實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans to implement.

**Goal:** 在 Stock Tracker Web GUI 中加入第 6 個分頁「📰 新聞」，顯示每日財經頭條，支援後端分頁與手動重新整理（20 分鐘冷卻）。

**Architecture:** 新增 `core/news.py` 負責從 Yahoo Finance API 抓取新聞並快取；`web/api.py` 新增 REST 端點；前端 `app.js` 新增分頁邏輯。資料流：前端 → API → news.py → Yahoo Finance API → news_cache.json。

**Tech Stack:** Python 3.11, Flask, Yahoo Finance v1 API, JSON cache

---

## 檔案結構

| 檔案 | 角色 |
|------|------|
| `core/news.py` | 新聞抓取、快取、分頁、冷卻邏輯 |
| `tests/test_news.py` | news.py 的測試 |
| `web/api.py:252-308` | 新增 `GET /api/news` 和 `POST /api/news/refresh` |
| `web/templates/base.html` | 加入第 6 個分頁按鈕 |
| `web/static/app.js` | 新聞分頁 UI 邏輯（載入、分頁、重新整理） |

---

## Task 1: core/news.py — 新聞核心模組

**Files:**
- Create: `stock-tracker/core/news.py`
- Test: `stock-tracker/tests/test_news.py`

**Interfaces:**
- Produces: `init_cache(cache_dir)`, `fetch_market_news()`, `get_news_page(page, per_page)`, `refresh_news()`, `get_cooldown_remaining()`

---

### Step 1.1: Write the failing test

```python
import pytest
import json
import time
from pathlib import Path
from core.news import (
    init_cache, fetch_market_news, get_news_page,
    refresh_news, get_cooldown_remaining, CACHE_TTL, REFRESH_COOLDOWN
)


def test_init_cache_creates_directory(tmp_path):
    cache_dir = str(tmp_path / "news")
    init_cache(cache_dir)
    assert Path(cache_dir).exists()


def test_get_news_page_returns_paginated_results(tmp_path):
    init_cache(str(tmp_path))
    page = get_news_page(page=1, per_page=10)
    assert "articles" in page
    assert "total" in page
    assert "page" in page
    assert "per_page" in page
    assert "total_pages" in page
    assert "last_refresh_at" in page
    assert "cooldown_remaining" in page
    assert len(page["articles"]) <= 10


def test_get_news_page_page_2(tmp_path):
    init_cache(str(tmp_path))
    page1 = get_news_page(page=1, per_page=20)
    page2 = get_news_page(page=2, per_page=20)
    assert page1["page"] == 1
    assert page2["page"] == 2
    if page1["total"] > 20:
        assert page2["articles"] != page1["articles"]


def test_refresh_news_cooldown(tmp_path):
    init_cache(str(tmp_path))
    result = refresh_news()
    assert "articles" in result or "error" in result
    # Second call within cooldown should fail
    result2 = refresh_news()
    if "articles" in result2:
        # If first call was slow enough to pass cooldown, retry logic
        pass
    else:
        assert "error" in result2
        assert "cooldown_remaining" in result2


def test_get_cooldown_returns_zero_when_not_refreshed(tmp_path):
    init_cache(str(tmp_path))
    remaining = get_cooldown_remaining()
    assert remaining == 0


def test_cache_persists_articles(tmp_path):
    init_cache(str(tmp_path))
    page1 = get_news_page(page=1, per_page=100)
    total = page1["total"]
    # Second call should return cached data
    page2 = get_news_page(page=1, per_page=100)
    assert page2["total"] == total
```

---

### Step 1.2: Run test to verify it fails

```bash
cd stock-tracker
python -m pytest tests/test_news.py -v
```

Expected: FAIL with "ModuleNotFoundError: No module named 'core.news'" or similar.

---

### Step 1.3: Write minimal implementation

```python
import json
import time
from pathlib import Path
from urllib.request import urlopen, Request
from urllib.error import URLError

CACHE_TTL = 1800
REFRESH_COOLDOWN = 1200
PER_PAGE = 20

_CACHE_PATH = None
_CACHE = None


def init_cache(cache_dir):
    global _CACHE_PATH, _CACHE
    _CACHE_PATH = Path(cache_dir) / "news_cache.json"
    _CACHE = {"articles": [], "last_refresh_at": 0, "cached_at": 0, "total": 0}
    _CACHE_PATH.parent.mkdir(parents=True, exist_ok=True)
    if _CACHE_PATH.exists():
        try:
            data = json.loads(_CACHE_PATH.read_text(encoding="utf-8"))
            _CACHE.update(data)
        except (json.JSONDecodeError, OSError):
            pass


def _save_cache():
    if _CACHE_PATH:
        _CACHE_PATH.parent.mkdir(parents=True, exist_ok=True)
        _CACHE_PATH.write_text(
            json.dumps(_CACHE, ensure_ascii=False, indent=2),
            encoding="utf-8",
        )


def _is_cache_valid():
    now = time.time()
    return (now - _CACHE.get("cached_at", 0)) < CACHE_TTL and len(_CACHE.get("articles", [])) > 0


def _is_cooldown():
    now = time.time()
    last = _CACHE.get("last_refresh_at", 0)
    return (now - last) < REFRESH_COOLDOWN


def _fetch_yahoo_news():
    symbols = "^TWII,2330.TW,2317.TW,2454.TW,2308.TW,2884.TW,2881.TW,2002.TW,2412.TW,1301.TW"
    url = f"https://query1.finance.yahoo.com/v1/finance/news?symbols={symbols}"
    req = Request(url, headers={
        "User-Agent": (
            "Mozilla/5.0 (Windows NT 10.0; Win64; x64) "
            "AppleWebKit/537.36 (KHTML, like Gecko) "
            "Chrome/120.0.0.0 Safari/537.36"
        ),
    })
    try:
        with urlopen(req, timeout=10) as resp:
            data = json.loads(resp.read().decode("utf-8"))
    except (URLError, OSError, json.JSONDecodeError):
        return []
    items = data.get("items", [])
    articles = []
    for item in items:
        articles.append({
            "title": item.get("title", ""),
            "summary": item.get("summary", ""),
            "url": item.get("link", ""),
            "source": "Yahoo Finance",
            "published": item.get("pubDate", ""),
            "related_stocks": item.get("relatedTickers", []),
        })
    articles.sort(key=lambda a: a.get("published", ""), reverse=True)
    return articles


def fetch_market_news():
    if _is_cache_valid():
        return _CACHE.get("articles", [])
    articles = _fetch_yahoo_news()
    _CACHE["articles"] = articles
    _CACHE["total"] = len(articles)
    _CACHE["cached_at"] = time.time()
    _save_cache()
    return articles


def get_news_page(page=1, per_page=20):
    articles = fetch_market_news()
    total = len(articles)
    total_pages = max(1, (total + per_page - 1) // per_page)
    start = (page - 1) * per_page
    end = start + per_page
    page_articles = articles[start:end]
    return {
        "articles": page_articles,
        "total": total,
        "page": page,
        "per_page": per_page,
        "total_pages": total_pages,
        "last_refresh_at": _CACHE.get("last_refresh_at", 0),
        "cooldown_remaining": get_cooldown_remaining(),
    }


def get_cooldown_remaining():
    if not _is_cooldown():
        return 0
    last = _CACHE.get("last_refresh_at", 0)
    return int(REFRESH_COOLDOWN - (time.time() - last))


def refresh_news():
    if _is_cooldown():
        remaining = get_cooldown_remaining()
        return {"error": f"冷卻中，請於 {remaining // 60} 分鐘後再試", "cooldown_remaining": remaining}
    articles = _fetch_yahoo_news()
    _CACHE["articles"] = articles
    _CACHE["total"] = len(articles)
    _CACHE["last_refresh_at"] = time.time()
    _CACHE["cached_at"] = time.time()
    _save_cache()
    return get_news_page(page=1)
```

---

### Step 1.4: Run test to verify it passes

```bash
cd stock-tracker
python -m pytest tests/test_news.py -v
```

Expected: All tests PASS (or some skipped due to network availability).

---

### Step 1.5: Commit

```bash
git add stock-tracker/core/news.py stock-tracker/tests/test_news.py
git commit -m "feat: add news core module with cache, pagination, cooldown"
```

---

## Task 2: 新增 API 路由

**Files:**
- Modify: `stock-tracker/web/api.py` (在檔案末尾，`# ====== Backtest ======` 之前)

**Interfaces:**
- Consumes: `core/news.py` 的 `init_cache`, `get_news_page`, `refresh_news`, `get_cooldown_remaining`
- Produces: `GET /api/news`, `POST /api/news/refresh`

---

### Step 2.1: Write the failing test

在 `tests/` 新增 API 測試或手動測試。先確認 API 不存在：

```bash
curl -s http://127.0.0.1:5000/api/news | python -c "import sys; print(sys.stdin.read())"
```

### Step 2.2: Add API routes

在 `web/api.py` 的 import 區塊加入：

```python
from core.news import init_cache as init_news_cache, get_news_page, refresh_news, get_cooldown_remaining
```

在 `init_history_dir(str(DATA_DIR))` 下方加入：

```python
init_news_cache(str(DATA_DIR))
```

在檔案末尾、`# ====== Backtest ======` 之前加入：

```python
# ====== News ======

@api.route("/news")
def get_news():
    page = request.args.get("page", 1, type=int)
    per_page = request.args.get("per_page", 20, type=int)
    per_page = min(per_page, 50)
    return jsonify(get_news_page(page=page, per_page=per_page))


@api.route("/news/refresh", methods=["POST"])
def refresh_news_api():
    result = refresh_news()
    if "error" in result:
        return jsonify(result), 429
    return jsonify(result)
```

---

### Step 2.3: Test API

```bash
# 在另一個終端機啟動 server
cd stock-tracker
python main.py web

# 在新終端機測試
curl http://127.0.0.1:5000/api/news?page=1
# 預期：回傳 JSON with articles, total, page, total_pages

curl -X POST http://127.0.0.1:5000/api/news/refresh
# 預期：成功或 429（冷卻中）
```

### Step 2.4: Commit

```bash
git add stock-tracker/web/api.py
git commit -m "feat: add news API endpoints (GET /api/news, POST /api/news/refresh)"
```

---

## Task 3: 前端 — 加入新聞分頁

**Files:**
- Modify: `stock-tracker/web/templates/base.html`
- Modify: `stock-tracker/web/static/app.js`

---

### Step 3.1: 更新 base.html

在 `base.html` 的 `.tab-nav` 中，`⚙️設定` 按鈕**之前**加入：

```html
<button class="tab-btn" data-tab="news">
    <span class="icon">📰</span>新聞
</button>
```

在 `tab-content` 區塊中，`<div id="tab-settings"` **之前**加入：

```html
<div id="tab-news" class="tab-content">
    <div class="page-title">📰 財經頭條</div>
    <div class="action-bar">
        <button class="btn btn-primary" onclick="refreshNews()">🔄 重新整理</button>
    </div>
    <div id="news-status" style="font-size:13px;color:var(--gray-500);margin-bottom:12px"></div>
    <div id="news-list"></div>
    <div id="news-pagination" class="pagination"></div>
</div>
```

---

### Step 3.2: 更新 app.js

在檔案末尾、`// ====== Init ======` 之前加入：

```javascript
// ====== News ======
let _newsCurrentPage = 1;

async function loadNews(page) {
    _newsCurrentPage = page || 1;
    const query = `/api/news?page=${_newsCurrentPage}&per_page=20`;
    try {
        const res = await fetch(query);
        const data = await res.json();
        if (!res.ok) { showToast(data.error || '載入失敗', 'error'); return; }
        renderNews(data);
    } catch (e) {
        showToast('新聞載入失敗: ' + e.message, 'error');
    }
}

function renderNews(data) {
    const el = document.getElementById('news-list');
    const statusEl = document.getElementById('news-status');

    // 冷卻狀態
    if (data.cooldown_remaining > 0) {
        const min = Math.ceil(data.cooldown_remaining / 60);
        statusEl.textContent = `🔄 已更新 · 還剩 ${min} 分鐘可重新整理`;
    } else {
        statusEl.textContent = `📅 上次更新：${data.last_refresh_at ? new Date(data.last_refresh_at * 1000).toLocaleString('zh-TW') : '尚未更新'}`;
    }

    if (!data.articles || data.articles.length === 0) {
        el.innerHTML = '<div style="text-align:center;padding:40px;color:var(--gray-400)">暫無新聞</div>';
        document.getElementById('news-pagination').innerHTML = '';
        return;
    }

    let html = '';
    for (const a of data.articles) {
        const date = a.published ? new Date(a.published).toLocaleDateString('zh-TW') : '';
        const stocks = a.related_stocks && a.related_stocks.length > 0
            ? '<span style="font-size:12px;color:var(--gray-500)">' + a.related_stocks.join(', ') + '</span>'
            : '';
        html += `
            <div class="news-item" onclick="window.open('${a.url}','_blank')" style="cursor:pointer;padding:14px;border:1px solid var(--border);border-radius:8px;margin-bottom:10px;transition:background 0.2s" onmouseover="this.style.background='var(--bg-hover)'" onmouseout="this.style.background=''">
                <div style="display:flex;justify-content:space-between;align-items:flex-start">
                    <div style="font-weight:600;color:var(--text);margin-bottom:4px">${a.title}</div>
                    <div style="font-size:12px;color:var(--gray-400);white-space:nowrap;margin-left:12px">${date}</div>
                </div>
                <div style="font-size:13px;color:var(--gray-500);margin-bottom:4px">${a.summary}</div>
                <div style="font-size:12px;color:var(--gray-400)">📎 ${a.source}</div>
                ${stocks ? '<div style="margin-top:4px">' + stocks + '</div>' : ''}
            </div>
        `;
    }
    el.innerHTML = html;

    // 分頁
    renderNewsPagination(data);
}

function renderNewsPagination(data) {
    const el = document.getElementById('news-pagination');
    if (data.total_pages <= 1) { el.innerHTML = ''; return; }

    let html = '<div style="text-align:center;padding:12px">';
    if (data.page > 1) {
        html += `<button class="btn btn-sm btn-gray" onclick="loadNews(${data.page - 1})" style="margin-right:8px">← 上一頁</button>`;
    }
    html += `<span style="font-size:13px;color:var(--gray-500);margin:0 12px">第 ${data.page} / ${data.total_pages} 頁</span>`;
    if (data.page < data.total_pages) {
        html += `<button class="btn btn-sm btn-gray" onclick="loadNews(${data.page + 1})" style="margin-left:8px">下一頁 →</button>`;
    }
    html += '</div>';
    el.innerHTML = html;
}

async function refreshNews() {
    const btn = document.querySelector('.tab-content.active .btn-primary');
    if (btn) btn.disabled = true;
    try {
        const res = await fetch('/api/news/refresh', { method: 'POST' });
        const data = await res.json();
        if (!res.ok) {
            if (data.cooldown_remaining) {
                const min = Math.ceil(data.cooldown_remaining / 60);
                showToast(`冷卻中，剩 ${min} 分鐘`, 'warning');
            } else {
                showToast(data.error || '重新整理失敗', 'error');
            }
            return;
        }
        showToast('新聞已更新', 'success');
        renderNews(data);
    } catch (e) {
        showToast('重新整理失敗: ' + e.message, 'error');
    } finally {
        if (btn) btn.disabled = false;
    }
}
```

---

### Step 3.3: 更新 switchTab 函數

在 `app.js` 的 `switchTab` 函數中，加入：

```javascript
if (tab === 'news') loadNews(1);
```

---

### Step 3.4: 測試前端

```bash
# 重啟 server
# 切換到「📰 新聞」分頁
# 確認新聞列表正常顯示
# 點擊下一頁 → 切頁
# 點擊重新整理 → 確認冷卻機制
```

### Step 3.5: Commit

```bash
git add stock-tracker/web/templates/base.html stock-tracker/web/static/app.js
git commit -m "feat: add news tab to web GUI with pagination and refresh cooldown"
```

---

## Task 4: 整合測試與驗證

### Step 4.1: 完整測試流程

```bash
# 停掉舊 server
Get-Process -Name python -ErrorAction SilentlyContinue | Stop-Process -Force

# 重新啟動
cd stock-tracker
python main.py web

# 測試 API
curl http://127.0.0.1:5000/api/news?page=1
# ✅ 確認回傳 JSON 含 articles, total, page, total_pages

curl -X POST http://127.0.0.1:5000/api/news/refresh
# ✅ 確認成功或 429（冷卻中）

# 測試 unit tests
python -m pytest tests/test_news.py -v
# ✅ 確認全部通過
```

---

## 驗收清單

- [ ] 第 6 個「📰 新聞」分頁出現且可點擊
- [ ] 進入分頁自動載入新聞列表
- [ ] 新聞列表顯示：標題、摘要、日期、來源
- [ ] 點擊新聞標題開啟外部連結
- [ ] 每頁顯示 20 則
- [ ] 下一頁/上一頁切換正常（後端分頁）
- [ ] 重新整理按鈕冷卻機制正常
- [ ] 冷卻中按鈕可操作但 API 限制
- [ ] 快取 30 分鐘有效
